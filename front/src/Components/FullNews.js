/* import React from "react";
import Page from "../Components/Page";
import "./News.js";
import { Link, Route } from 'react-router-dom'

const AllNews = [
    { id:'none',
      title:'not found',
    },
  
    { id:'news-1',
    },
    { id:'news-2',  
    },
  
    { id:'news-3',
    },
  
    { id:'news-4',
    },
  
    { id:'news-5',
    }
  ]

export default () => (
    <Page className="all-news">



const LeaguePage = ({ report }) => {
  const id = (report.params.newsName)
  const news = AllNews.find( news => news.id === id ) || AllNews[0]
  const { title, text } = news
  return (<div>
      <h3>{title}</h3>
      
  </div>)
}
  </Page>

); */