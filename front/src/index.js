
    /**
 * This is the entry point of your app.
 * This is where everything happens
 */
import { render } from "react-dom";
import React from "react";
import App from "./App";
import './index.css'

/**
 * Insert the app in the HTML document
 */
render(<App />, document.getElementById("root"));
